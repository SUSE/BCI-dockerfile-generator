%if "%_repository" == "images"
Type: kiwi
Repotype: none
Patterntype: none

%else
%if %_repository == "containerfile"
Type: docker
BuildEngine: podman

Substitute: system-packages:podman podman buildah createrepo_c release-compare container-build-checks-vendor-openSUSE container-build-checks-strict skopeo umoci post-build-checks

BuildFlags: container-timestamp:0

%else
Type: spec
Repotype: none
Patterntype: none

%endif
%endif

Prefer: openSUSE-release-appliance

# don't rebuild with every release:
# Substitute: distribution-release openSUSE-release-mini
